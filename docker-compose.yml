services:
  mongo:
    image: mongodb/mongodb-community-server:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:

auth:
  build: ./auth
  ports:
    - "3001:3001"
  env_file:
    - ./auth/.env
  depends_on:
    - mongo